package Pages.WidgetsPage;

import org.openqa.selenium.*;
import org.openqa.selenium.support.ui.*;

public class ToolsTipsPage {
    WebDriver driver;
    WebDriverWait wait;

    public ToolsTipsPage(WebDriver driver, WebDriverWait wait) {
        this.driver = driver;
        this.wait = wait;
    }
}
