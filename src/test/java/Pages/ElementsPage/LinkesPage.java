package Pages.ElementsPage;

import org.openqa.selenium.*;
import org.openqa.selenium.support.ui.*;

public class LinkesPage {
    WebDriver driver;
    WebDriverWait wait;

    public LinkesPage(WebDriver driver, WebDriverWait wait) {
        this.driver = driver;
        this.wait = wait;
    }
}
